In Order to Run the page at its full potential, An internet connection is needed.
-> this is because some fonts are in the bootstrap library.

Instructions.
1. Extract the ZIP file 9433ACourseWebsite.ZIP
A directory and 2 files will be extracted.
CourseWebsite 
Prelim CommitLog 
peer.jpg

2. 
Option A: -->Transfer the CourseWebsite directory to www directory in WAMP because 
			Respond.js doesn't work if you view the page via file://
			--> Run your wamp server application.
Option B: Running the index.html to a web browser can also be done to run the coursewebsite.
			but running it in a local wamp server is recommended.
3. If you Chose Option A, open up the browser and type in localhost/CourseWebsite/
	